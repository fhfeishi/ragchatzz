# 把所有 .ipynb 当二进制处理，禁止文本 diff 和换行符转换
*.ipynb binary 

# 统一文本文件使用 LF
* text=auto eol=lf

# 特殊文件（如 Windows 脚本）用 CRLF
*.bat text eol=crlf
*.ps1 text eol=crlf

# 锁定某些文件不转换
package.json  text eol=lf
package-lock.json  text eol=lf 